# Semiont Demo Configuration
# Copy this file to .env and customize for your environment

# Semiont Server URL (via Envoy on port 8080)
# For local development: http://localhost:8080
# For Codespaces: https://{codespace-name}-8080.app.github.dev
SEMIONT_URL=http://localhost:8080

# Authentication credentials
# Get these from your Semiont setup or use setup-demo.sh to create a demo account
AUTH_EMAIL=you@example.com
AUTH_PASSWORD=your-password-here

# Optional: Use an access token instead of email/password
# ACCESS_TOKEN=your-jwt-token-here

# Data directory for staging downloads before upload to Semiont
# Used for temporary storage of fetched content (e.g., from HuggingFace, arXiv)
DATA_DIR=data

# Log level for debugging (default: debug)
# Options: error, warn, info, debug
LOG_LEVEL=debug